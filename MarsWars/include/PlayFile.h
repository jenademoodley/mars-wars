#ifndef PLAYFILE_H
#define PLAYFILE_H
 #include <string>

class PlayFile
{
    public:
        PlayFile();
        PlayFile(std::string);
        virtual ~PlayFile();
    protected:
    private:
};

#endif // PLAYFILE_H
